on:
  push:

name: Test makepkg on ubuntu

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: apt update && apt install makepkg && ./packaging/aur.sh v0.3.1-beta

